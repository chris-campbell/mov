import React from "react";
import { Imports } from ".";

const WatchList = () => {
  const { BackButton } = Imports;

  return (
    <div>
      <BackButton />
    </div>
  );
};

export default WatchList;
