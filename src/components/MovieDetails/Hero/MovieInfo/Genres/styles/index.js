import styled from "styled-components";

const GenresContainer = styled.div`
  display: flex;
  gap: 1rem;
  color: #ffffff;
  margin: 1rem 0;
  flex-wrap: wrap;

  .genre {
    font-size: 0.9rem;
  }
`;

export default GenresContainer;
